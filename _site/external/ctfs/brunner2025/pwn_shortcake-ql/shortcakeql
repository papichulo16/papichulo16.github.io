#!/usr/bin/env python3
import sqlite3
conn = sqlite3.connect(":memory:")

print("Gib SQL pl0x end with EOF:")
def get_input():
    a = ""
    while True:
        tmp = input()
        if "EOF" in tmp:
            break
        a += tmp
    return a

a = get_input()
conn.executescript(a)
